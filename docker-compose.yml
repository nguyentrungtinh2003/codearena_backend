version: "3.8"

services:
  mysql:
    image: mysql:8
    container_name: code-arena-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: code_arena_mysql
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    image: nguyentrungtinh/code-arena-backend:${IMAGE_TAG}
    container_name: code-arena-backend
    ports:
      - "8081:8081"
    depends_on:
      - mysql

volumes:
  mysql_data:
